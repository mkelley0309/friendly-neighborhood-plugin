__pycache__/
*.pyc
.DS_Store

# dev-env runtime artifacts (not part of the plugin deliverable).
# Anchored to repo root so it never matches plugins/*/templates/.claude/.
/.claude/logs/
# personal local overrides, wherever they appear (templates ship settings.json, not .local)
.claude/settings.local.json
