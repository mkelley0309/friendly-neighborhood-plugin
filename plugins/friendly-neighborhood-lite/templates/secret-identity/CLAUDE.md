# Secret Identity — contract pointer

The operating contract for this subtree lives in `.claude/rules/secret-identity.md`, which auto-loads when you work on files here. Scoped subagents: read that rule before acting.
